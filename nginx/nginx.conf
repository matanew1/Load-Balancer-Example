# Define event handling parameters
events {
    worker_connections 1024;
}

# Define server configuration
http {
    # Define backend server group
    upstream localhost {
        # References to backend containers
        server app1:5000;
        server app2:5000;
        server app3:5000;
        server app4:5000;
        server app5:5000;
    }

    # Define server configuration
    server {
        listen 80;
        server_name localhost;

        # Define how requests should be handled
        location / {
            proxy_pass http://localhost;
            proxy_set_header Host $host;
        }
    }
}
